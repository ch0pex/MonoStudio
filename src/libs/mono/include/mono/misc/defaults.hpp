#pragma once

#ifdef MONO_USE_CPP_MODULES
import std;
#else
#include <cstddef>
#endif

namespace mono::defaults {

// --- Windows ---
// inline constexpr std::size_t supported_windows = 5;

} // namespace mono::defaults
